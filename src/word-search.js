
const createPuzzle = input =>
  input.split("\n")
    .map((line, y) => line.split(","))

const candidatesFor = () => {}

const find = () => {}

const solve = input => {}

module.exports = {
  createPuzzle,
  candidatesFor,
  find,
  solve,
}